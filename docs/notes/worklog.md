2026-08-15 — fix/share-shade-predicate — PR #17 — shared the shade pixel predicate between routing and assistant probes, corrected CLAUDE.md invariant #5, and verified with `npm test`, `npx tsc --noEmit`, and `npm run build`; assumed no per-directory app/lib guide because `app/lib/CLAUDE.md` is missing and recorded that under doc drift.
2026-08-15 — fix/route-progress-status — PR #18 — added route-calculation phase/count progress for graph fetch, shade sampling, and multi-stop leg work, split the remaining P0 streaming/partial-results backlog, and verified with `npm test`, `npx tsc --noEmit`, `npm run build`, and `npm run dev` HTTP checks; assumed interactive browser verification is unavailable because no Chromium/Playwright binary is installed.
2026-08-15 — fix/partial-route-results — PR #19 — returned honest partial multi-stop walking routes when a later leg fails, guarded partial routes from save/export/start actions, and verified with `npm test`, `npx tsc --noEmit`, `npm run build`, and `npm run dev` HTTP checks; interactive browser verification remains unavailable without Chromium/Playwright.
2026-08-15 — fix/stream-route-preview — PR #20 — streamed a temporary multi-stop route preview to the existing map route layer after each completed first-pass leg, then cleared it when final options land, and verified with `npm test`, `npx tsc --noEmit`, `npm run build`, and `npm run dev` HTTP checks; interactive browser verification remains unavailable without Chromium/Playwright.
2026-08-15 — fix/shade-query-without-camera — PR #21 — added a conservative cached-geometry shade point query and made `check_shade` use it without camera movement when possible, keeping the old camera sampler as fallback; verified with `npm test`, `npx tsc --noEmit`, `npm run build`, and `npm run dev` HTTP checks, with interactive browser verification still unavailable without Chromium/Playwright.
2026-08-15 — fix/offscreen-shade-query — PR #22 — removed the `check_shade` camera fallback by fetching offscreen OSM building way footprints through the Overpass proxy and computing point shade from sun position, with tests covering footprint parsing and no-map assistant shade checks; verified with `npm test`, `npx tsc --noEmit`, `npm run build`, and `npm run dev` HTTP checks, and recorded the multipolygon-relation limitation as remaining P0 trust work.
2026-08-15 — fix/offscreen-building-relations — PR #23 — added multipolygon relation parsing for offscreen building shade queries by requesting relation geometry and stitching outer members into closed rings, closing the remaining P0 shade-query trust gap; verified with `npm test`, `npx tsc --noEmit`, `npm run build`, and `npm run dev` HTTP checks.
2026-08-15 — perf/baseline-metrics — PR #24 — recorded a cold build artifact-size baseline in `docs/notes/performance-baseline.md`; assumed throttled-4G TTI and representative route-calc timings cannot be honestly captured in this workspace because Chromium/Playwright/Lighthouse are unavailable, so the backlog now tracks that remaining baseline slice.
2026-08-15 — perf/cache-overpass-buildings — PR #25 — cached offscreen building-footprint Overpass queries for repeated assistant shade probes, returned cloned cached geometry to avoid mutation leaks, and verified with `npm test -- app/lib/__tests__/overpass.test.ts`, `npx tsc --noEmit`, `npm test`, and `npm run build`.
2026-08-15 — perf/cache-station-entrances — PR #26 — cached station-entrance Overpass queries for repeated transit-capable route calculations, returned cloned entrance objects to avoid mutation leaks, and verified with `npm test -- app/lib/__tests__/overpass.test.ts`, `npx tsc --noEmit`, `npm test`, and `npm run build`.
