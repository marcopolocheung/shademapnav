# AGENTS.md

Canonical agent guide: **`CLAUDE.md`** at the repo root — read it first.
It contains commands, hard invariants, the repo map, and a task→edit-point routing table.

Topic docs (read one, just-in-time): `docs/kb/INDEX.md`.
Per-directory notes: nearest `CLAUDE.md` under `app/*` and `api/`.

Do not read `.worktrees/` (orphaned stale checkout). The previous auto-generated KB
was archived to `docs/kb/_archive/opencode-2026-06-02/` — do not trust its contents.
